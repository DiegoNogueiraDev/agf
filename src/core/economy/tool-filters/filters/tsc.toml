# tsc — TypeScript compiler errors grouped by file
[[filters]]
name = "tsc"
priority = 30
detect = ["error TS\\d+:", "Found \\d+ error"]

[filters.pipeline]
strip_ansi = true
strip_lines = ["^node_modules/"]
max_lines = 200
on_empty = "no type errors"

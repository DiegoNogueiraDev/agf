[[filters]]
name = "tofu-plan"
priority = 50
detect = ["OpenTofu will perform", "Plan:", "No changes", "Tofu has compared"]

[filters.pipeline]
strip_ansi = true
strip_lines = ["^$", "^Reading...", "^Refreshing state"]
replace = [["\\s+=>\\s+", " -> "]]
max_lines = 400

---
name: ui-ux-pro-max
description: UI/UX engineering for the agent-graph-flow lifecycle via the `agf` CLI. Use for frontend/UI work; load context first.
---

# ui-ux-pro-max

Use within the agent-graph-flow lifecycle. Drive everything via the `agf` CLI — zero MCP. Load context with `agf context <id>` before changing anything.

## Codex Notes

- In Codex Plan Mode, plan only — do not mutate files.
- During implementation, follow `AGENTS.md` and use `apply_patch` for manual edits.

{
  "id": "economy-abs",
  "tier": "T0",
  "persona": "dev",
  "prd": "# Task: implementar abs\n\n## Task: implementar abs(n)\nAdicione `exports.abs = (n) => n < 0 ? -n : n` em `math.js` (valor absoluto de inteiro).\n\n### Acceptance Criteria\n- `abs(5)` retorna `5`\n- `abs(-3)` retorna `3`\n- `abs(0)` retorna `0`",
  "testCmd": "node --test",
  "tokenBudget": 4000,
  "expectedResolve": true,
  "tags": ["economy", "atomic", "cost-baseline"]
}
